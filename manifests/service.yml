apiVersion: v1
kind: Service
metadata:
    name: "dileepkub1-0f99"
    labels:
        app: "dileepkub1-0f99"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "dileepkub1-0f99"